# json.extract! article, :title, :body, :source, :user, :id


  json.title article.title
  json.body article.body
  json.soruce article.source
  json.username article.user.username
  json.id article.id
